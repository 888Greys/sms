{
  "name": "sms-batch-telegram-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit",
    "start": "node dist/src/bot.js",
    "pm2:start": "pm2 start ecosystem.config.cjs --only sms-bot --env production",
    "pm2:restart": "pm2 restart sms-bot",
    "pm2:logs": "pm2 logs sms-bot",
    "pm2:stop": "pm2 stop sms-bot",
    "pm2:delete": "pm2 delete sms-bot",
    "test": "npm run build && node --test dist/tests",
    "import:batches": "node scripts/import-batches.mjs"
  },
  "dependencies": {
    "@upstash/redis": "^1.35.6",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
