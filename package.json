{
  "name": "sms-batch-telegram-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "import:batches": "node scripts/import-batches.mjs",
    "set:webhook": "node scripts/set-webhook.mjs"
  },
  "dependencies": {
    "@upstash/redis": "^1.35.6",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@netlify/functions": "^3.1.12",
    "typescript": "^5.9.2"
  }
}
